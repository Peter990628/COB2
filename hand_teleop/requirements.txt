# 핵심 버전을 고정해 다른 팀원 컴퓨터에서도 같은 환경을 재현합니다.
mediapipe==0.10.35
numpy==2.2.6
opencv-contrib-python==5.0.0.93

# Ubuntu 22.04의 기본 Matplotlib는 NumPy 2.x와 바이너리 호환되지 않습니다.
# venv 내부에 NumPy 2.x 호환 버전을 설치해 MediaPipe import 오류를 방지합니다.
matplotlib==3.10.9

# 현재 RG2 코드가 pymodbus.client.sync API를 사용하므로 2.5.3을 유지합니다.
pymodbus==2.5.3
